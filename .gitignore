node_modules/
dist/
config.json
auth.json
# The seed target list. Publishing which cities this service pulls at high zoom
# hands an observer the exact traffic pattern to watch for on the downloading
# network, which is the correlation the Tor transport exists to break.
# hot-zones.example.json carries the structure.
hot-zones.json
# The bulk coverage plan. Same reasoning: what this server pulls is a
# fingerprint of the service.
regions.json
maps.db
maps.db-shm
maps.db-wal
# Pre-rename database. Still ignored so an install that has not started since
# the rename cannot commit one by accident.
maps-manager.db
maps-manager.db-shm
maps-manager.db-wal
land.json
land-mask.bin
logs/
*.local
.DS_Store

# Superseded by the SQLite database; imported once on first start.
maps.json
coverages.json
runs.json
